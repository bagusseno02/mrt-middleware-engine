# Use together with compose.yaml after setting serial_port in config.yaml.
services:
  collector:
    devices:
      - /dev/ttyUSB0:/dev/ttyUSB0
    group_add:
      - "${SERIAL_GID:?Set SERIAL_GID to the device group ID}"
